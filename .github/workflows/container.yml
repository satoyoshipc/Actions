name: container01
on: push

jobs:
  node-docker:
    runs-on: ubuntu-latest
    container: #起動するコンテナイメージを指定
      image: node:13.5.0-alpine3.10
    steps:
      - name: Log node version
        run: |
          node -v 
          cat /etc/os-release
          